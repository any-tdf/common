// ProgressLoop SVG 数据只保存稳定坐标，动态半径和 stroke 继续由派生层计算。
// ProgressLoop SVG data stores only stable coordinates; dynamic radius and stroke stay in the derived layer.
export const progressLoopSvg = {
	viewBox: '0 0 24 24',
	center: 12
} as const;
